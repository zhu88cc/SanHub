# 更新日志

## [2024-12-09]

### 优化
- **视频生成页面布局优化**：将"全站剩余生成次数"移至页面标题右侧，与标题同行显示，不再挤压生成结果区域
- **管理后台页面整合**：移除 `/admin/api` 页面中重复的 SORA 后台配置，改为链接跳转至 `/admin/tokens` 页面，避免配置分散

### 修复
- **API 路由缓存问题**：为 `/api/announcement` 和 `/api/sora/quota` 添加 `force-dynamic` 配置，确保每次请求都获取最新数据
